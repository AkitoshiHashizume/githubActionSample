name: Manual
on:
  workflow_dispatch:
    inputs:
      greeting:
        type: string
        description: sample
jobs:
  manual:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "${{inputs.greeting}}"
